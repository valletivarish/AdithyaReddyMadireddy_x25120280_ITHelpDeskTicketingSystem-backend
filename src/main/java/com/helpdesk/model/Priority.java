package com.helpdesk.model;

/**
 * Enum representing ticket priority levels.
 * Used to determine the urgency and ordering of tickets in the queue.
 */
public enum Priority {
    LOW,
    MEDIUM,
    HIGH,
    CRITICAL
}
