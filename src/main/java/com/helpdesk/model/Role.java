package com.helpdesk.model;

/**
 * Enum representing user roles in the helpdesk system.
 * ADMIN can manage all resources, AGENT handles tickets, USER submits tickets.
 */
public enum Role {
    ADMIN,
    AGENT,
    USER
}
